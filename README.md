# DomMarNoNicoTimer

A single-page countdown experience rebuilt as a small React + Vite app so it can be previewed locally on the development branch before being released.

## Run locally

1. Install dependencies
   - npm install
2. Start the dev server
   - npm run dev
3. Open the local URL shown in the terminal

## Build

- npm run build
